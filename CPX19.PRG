@ 17,1 CLEAR TO 19,79

***@ 17,1 TO 19,79

@ 18,2 SAY "PRESS OPTION NUMBER TO CONTINUE..."

CPX=" "
@ 18,77 GET CPX
READ
*
* COMPARE CPX WITH RIGHT ANSWER. FIELD RIGHTCOL CONTAINS COL WHERE RIGHT ANSWER IS
* AND SAVE POINTS TO DISK IN Q113A.DBF
* POINTS GAINED ARE SAVED IN FIELD POINTS N(99)

IF CPX="1"
	IF VAL(CPX)=RIGHTCOL
		REPLACE POINTS WITH 1
	ENDIF
ENDIF

IF CPX="2"
	IF VAL(CPX)=RIGHTCOL
		REPLACE POINTS WITH 1
	ENDIF
ENDIF

IF CPX="3"
	IF VAL(CPX)=RIGHTCOL
		REPLACE POINTS WITH 1
	ENDIF
ENDIF

IF CPX="4"
	IF VAL(CPX)=RIGHTCOL
		REPLACE POINTS WITH 1
	ENDIF
ENDIF

IF CPX="0"
	WEXITP=1
ENDIF

